"""Codex harness unit tests.

`seconds` and `write_database` moved to `tests/codex_state_db.py` so the
`state_<n>.sqlite` schema exists in exactly one place, shared with the
`codex_home` acceptance fixture in `tests/conftest.py`.
"""
